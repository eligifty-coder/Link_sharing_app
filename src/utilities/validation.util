

export const validatedLength = (value, minValue)=>{
    return value.length >=  minValue
}


export const validateEmail = (value) => {
    const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
    return regex.test(value)
}
